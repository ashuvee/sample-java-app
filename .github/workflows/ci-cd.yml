name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  JAVA_VERSION: '17'
  NEXUS_URL: 'http://3.85.143.154:8081'
  NEXUS_REPO: 'my-java-app'
  NEXUS_GROUP: 'com/web/app'
  NEXUS_ARTIFACT: 'my-app'
  SONAR_HOST_URL: 'http://100.25.217.210:9000'
  TOMCAT_URL: 'http://3.88.108.148:8080/manager/text'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📦 Checkout Code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: ☕ Set up JDK ${{ env.JAVA_VERSION }}
      uses: actions/setup-java@v4
      with:
        java-version: ${{ env.JAVA_VERSION }}
        distribution: 'temurin'
        cache: 'maven'

    - name: 🔍 SonarQube Analysis
      env:
        SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      run: |
        echo "Running SonarQube static analysis..."
        mvn clean verify sonar:sonar \
          -Dsonar.projectKey=sample-java-app \
          -Dsonar.host.url=${{ env.SONAR_HOST_URL }} \
          -Dsonar.login=$SONAR_TOKEN \
          -DskipTests

    - name: ⚙️ Build WAR Artifact
      run: |
        echo "Building WAR file..."
        mvn clean package -DskipTests
        echo "✅ Build Completed!"
        ls -lh target/*.war || true

    - name: 📤 Upload Artifact to Nexus
      env:
        NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
        NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
      run: |
        set -e
        WAR_FILE=$(ls target/*.war | head -1)
        FILE_NAME=$(basename "$WAR_FILE")
        VERSION="0.0.${{ github.run_number }}"
        
        echo "📤 Uploading $FILE_NAME to Nexus as version $VERSION..."
        
        curl -v -u ${NEXUS_USERNAME}:${NEXUS_PASSWORD} \
          --upload-file "$WAR_FILE" \
          "${NEXUS_URL}/repository/${NEXUS_REPO}/${NEXUS_GROUP}/${NEXUS_ARTIFACT}/${VERSION}/${NEXUS_ARTIFACT}-${VERSION}.war"
        
        echo "✅ Artifact uploaded successfully to Nexus!"

    - name: 🚀 Deploy to Tomcat
      env:
        NEXUS_USERNAME: ${{ secrets.NEXUS_USERNAME }}
        NEXUS_PASSWORD: ${{ secrets.NEXUS_PASSWORD }}
        TOMCAT_USERNAME: ${{ secrets.TOMCAT_USERNAME }}
        TOMCAT_PASSWORD: ${{ secrets.TOMCAT_PASSWORD }}
      run: |
        set -e
        cd /tmp
        rm -f *.war || true
        
        echo "⏳ Waiting for Nexus to index the artifact..."
        sleep 5
        
        echo "🔍 Fetching latest WAR from Nexus..."
        SEARCH_URL="${NEXUS_URL}/service/rest/v1/search?repository=${NEXUS_REPO}&group=com.web.app&name=my-app&sort=version"
        echo "Search URL: $SEARCH_URL"
        
        RESPONSE=$(curl -s -u ${NEXUS_USERNAME}:${NEXUS_PASSWORD} "$SEARCH_URL")
        echo "Nexus response: $RESPONSE"
        
        DOWNLOAD_URL=$(echo "$RESPONSE" | grep -oP '"downloadUrl"\s*:\s*"\K[^"]+\.war' | grep -vE '\.md5|\.sha1' | tail -1)
        
        if [[ -z "$DOWNLOAD_URL" ]]; then
          echo "❌ No WAR found in Nexus!"
          echo "Trying direct path method..."
          VERSION="0.0.${{ github.run_number }}"
          DOWNLOAD_URL="${NEXUS_URL}/repository/${NEXUS_REPO}/${NEXUS_GROUP}/${NEXUS_ARTIFACT}/${VERSION}/${NEXUS_ARTIFACT}-${VERSION}.war"
          echo "Direct URL: $DOWNLOAD_URL"
        fi
        
        echo "⬇️ Downloading WAR: $DOWNLOAD_URL"
        curl -f -u ${NEXUS_USERNAME}:${NEXUS_PASSWORD} -O "$DOWNLOAD_URL"
        WAR_FILE=$(basename "$DOWNLOAD_URL")
        APP_NAME=$(echo "$WAR_FILE" | sed 's/-[0-9].*//')
        
        echo "🧹 Removing old deployment..."
        curl -u ${TOMCAT_USERNAME}:${TOMCAT_PASSWORD} \
          "${TOMCAT_URL}/undeploy?path=/${APP_NAME}" || true
        
        echo "🚀 Deploying new WAR to Tomcat..."
        curl -u ${TOMCAT_USERNAME}:${TOMCAT_PASSWORD} \
          --upload-file "$WAR_FILE" \
          "${TOMCAT_URL}/deploy?path=/${APP_NAME}&update=true"
        
        echo "✅ Deployment successful! Application updated."

    - name: 📊 Summary
      if: always()
      run: |
        if [ ${{ job.status }} == 'success' ]; then
          echo "🎉 Pipeline completed successfully — Application live on Tomcat!"
        else
          echo "❌ Pipeline failed — Check GitHub Actions logs."
        fi
